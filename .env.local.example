# Copy this file to .env.local (never commit .env.local — it's gitignored).
#
# Local dev: run `bunx supabase start`, it prints these values (API URL + anon key).
# Real project: found in your Supabase project's Settings → API.

VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=
