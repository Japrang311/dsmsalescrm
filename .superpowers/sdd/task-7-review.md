# Task 7 Independent Review

Date: 2026-07-19
Verdict: **APPROVED**

## Confirmed compliant

- [Pasti] The post-report fix to `supabase/tests/account-lifecycle.test.ts` is correctly implemented, not just described. `beforeAll` (`:181-186`) calls `admin_active_super_admin_count()` and stores the result in `baselineActiveSuperAdminCount` *before* `createRoleFixtureUsers()` (`:188`) or `createSecondSuperAdmin()` (`:190`) run — the ambient bootstrap Super Admin (`77777777-...`) is captured in the baseline, not conflated with the fixture. The assertion at `:454` is `expect(data).toBe(baselineActiveSuperAdminCount + 1)`, matching exactly what the fix claims. `afterAll` (`:385`) resets `baselineActiveSuperAdminCount = undefined` alongside `fixtures`/`rows`/`secondSuperAdminId`, and nowhere in `afterAll` (`:332-398`) is the persistent bootstrap account (`77777777-...`) referenced or deleted — only fixture-created rows (`created.*` ids), `secondSuperAdminId`, and `disposableAuthIds` are cleaned up.

- [Pasti] `supabase/snippets/bootstrap_super_admin_role.sql` hard-fail behavior verified by reading the SQL directly, not trusting the report's claim: `:32-40` does `select u.email into target_email from auth.users u where u.id = target_uuid; if target_email is null then raise exception ...`. This unconditionally raises before the `insert ... on conflict` block ever runs (`:42-49`), so an unknown UUID always aborts the `do $$ ... $$` block with zero rows written — genuinely stricter than `bootstrap_manager_role.sql`, whose `insert ... select ... from auth.users u where u.id = target_uuid` (`:18-21`) silently produces zero inserted rows for an unknown UUID with no error at all. Confirmed idempotency: the `on conflict (id) do update` (`:44-49`) sets the same fixed literals (`role = 'super_admin'`, `account_status = 'active'`) plus `excluded.name/initials/email`, so a second run for the same UUID changes no identity, matching the report's rehearsal claim. Confirmed zero credentials/tokens/project URLs — the file only reads `auth.users.email` and writes `public.profiles`; no service-role key, password, or URL appears anywhere in the 50 lines.

- [Pasti] The two inspection-only lifecycle proofs are backed by code that actually supports the stated conclusion, not just plausible-sounding prose. In `supabase/migrations/20260718180929_add_account_lifecycle_functions.sql`, all three lifecycle functions place the self-action check strictly before the `LAST_ACTIVE_SUPER_ADMIN` guard: `change_team_member_role` — `SELF_ROLE_CHANGE_FORBIDDEN` at `:578` precedes the `active_super_admin_count() <= 1` check at `:583-588`; `set_team_member_status` — `SELF_DEACTIVATION_FORBIDDEN` at `:671` precedes the guard at `:675-678`; `delete_eligible_account` — `SELF_DELETE_FORBIDDEN` at `:766` precedes the guard at `:769-772`. Since every one of these functions also requires `is_active_super_admin(p_actor_id)` up front (`ACTIVE_SUPER_ADMIN_REQUIRED`, e.g. `:568-569`), the report's syllogism holds: if `active_super_admin_count() <= 1` is true at check time, the acting caller (who must themselves be that one active admin) can only be targeting themselves, and the self-check — which runs first in program order — already rejects that case. This is a real, traceable proof, not an assumption. Independently confirmed `private.transfer_active_ownership` (`:118-246`) performs all validation (`ADMINISTRATIVE_REASON_REQUIRED`, `OWNERSHIP_SOURCE_EQUALS_DESTINATION`, `ACTIVE_SUPER_ADMIN_REQUIRED` at `:167-168`, `INVALID_OWNERSHIP_SOURCE`, `INVALID_OWNERSHIP_DESTINATION`) before any of the three sequential `UPDATE` statements (`:181-201`), all inside one `plpgsql` function body — PL/pgSQL's implicit per-call transaction semantics do guarantee any later exception rolls back the earlier updates in the same invocation, so the report's atomicity argument is structurally sound, not just asserted.

- [Pasti] `profiles_select_own_or_privileged` fail-closed claim (report Step 4, item 5) verified directly: `supabase/migrations/20260718160405_add_account_status_and_active_role_guard.sql:30-36` shows the policy was redefined to `(id = (select auth.uid()) and account_status = 'active') or (select public.current_user_role()) in ('manager', 'executive', 'super_admin')` — an inactive user's own-row select genuinely requires `account_status = 'active'`, so the report's claim that a pre-deactivation JWT loses even its own-profile read is backed by the actual policy text, not just narrated.

- [Pasti] The `/login` auth-mix-up flagged in "Browser automation notes" item 2 was investigated at the code level as instructed. `src/routes/login.tsx` is a plain controlled-component form: `email`/`password` are `useState` values updated only via each `Input`'s `onChange={(e) => setEmail(e.target.value)}` (`:22-23,61-63,72-73`), and `handleSubmit` (`:27-41`) is a fresh closure defined in the component body on every render, so it always reads the current `email`/`password` state — no memoization, no stale closure, no module-level or global mutable state that could bleed between renders or across role switches. `role-context.tsx`'s real-session path (`loadRealSession()`, `:60-68`) only reads whatever `supabase.auth.getSession()` returns after the fact; it performs no credential handling itself and has no code path that could substitute one account's credentials for another's. There is no shared session-store, no race between two concurrent `signInWithPassword` calls, and no localStorage read that could resurrect a stale identity on this path (the `STORAGE_KEY` localStorage read at `:122-127` is scoped to the *dev switcher's* `DevRole`, and is explicitly bypassed once `authSource === "real"`). I found nothing at the code level that would cause a correctly-typed `rendra@dsm.co.id` submission to authenticate as `aditya@dsm.co.id` — the component has no mechanism for that outcome. This is consistent with (though does not prove) the report's autofill-artifact theory: browser password managers are known to set input values via a native DOM setter that can bypass React's synthetic `onChange` in some browser/extension combinations, leaving the displayed value in the field visually correct while React's `email`/`password` state (and therefore what actually gets submitted) still holds a previously-autofilled value. Given the report already flagged the same session as having a separate, confirmed browser-extension popup interference issue, and that switching to `form_input` (which sets React-controlled values directly, bypassing the autofill path) reportedly avoided the problem, the autofill explanation is plausible and no code fix is warranted based on this reading. Residual-risk note (not a Critical finding, per the task's explicit scoping): this was not dynamically re-driven, so a genuine `/login` defect cannot be 100% ruled out — a cheap follow-up would be to repeat the `rendra@dsm.co.id` login once in a clean browser profile with autofill disabled, but nothing in the code justifies blocking on it now.

- [Pasti] `docs/auth-bootstrap.md` claims are appropriately scoped to local-only verification. The status line (`:3`) says "verified against the local Supabase stack ... It has not been run against any remote project — remote use still requires separate approval naming the exact target." Every verification bullet in Step 3 (`:32-35`) is phrased as "verified locally" / "verified: ..." with the specific evidence (disposable UUID, idempotent rerun, unknown-UUID rejection), and nothing claims remote or production verification. No overclaiming found.

- [Pasti] `CLAUDE.md` Phase 12 status language is accurate and Phase 11 language is untouched. Diffed the current file against the brief's/system-prompt's quoted original: line 7 now reads "Phase 12 ... is locally verified complete as of 2026-07-19 — see `.superpowers/sdd/task-7-report.md` ..." and "Phase 11 ... is accepted and pending, and now proceeds on top of the verified four-role RLS foundation" — this exactly matches the report's claimed edit and does not touch the separate, still-intact "Accepted commercial document rules (Phase 11)" section (`:81-91`), which is byte-identical to the pre-Task-7 version quoted in the system prompt's project CLAUDE.md context. The RLS-boundary paragraph (`:11`), role/session paragraph (`:52`), "Accepted Super Admin rules" heading (`:54`) and closing bullet (`:63`) all consistently use present-tense "implemented, locally verified" language rather than "will add"/"pending," matching the report's Step 7 claim.

- [Pasti] `HANDOFF.md`'s "Suggested next steps for Codex" section (`:119-127`) was correctly updated: item 1 points at `.superpowers/sdd/task-7-report.md` and states Phase 12 is "locally verified complete as of 2026-07-19" rather than instructing the next agent to implement Tasks 38-41; item 2 tells the next agent to start Phase 11 directly ("Phase 11 (normalized commercial tables now start on top of the verified four-role RLS foundation)"). No stale to-do language describing completed Phase 12 work as pending remains in this section.

- [Pasti] `docs/superpowers/plans/2026-07-18-super-admin-team-role-management-implementation.md`'s Task 7 checkbox section (`:519-580` region) and `tasks/plan.md`'s Phase 12/Checkpoint 12 section (`:181-198`) both check off exactly what the report's evidence supports, with no unearned confidence: Step 4/Item 5 and Step 5 (last-active-admin, transactional-transfer) checkboxes carry explicit inline caveats matching the report's own hedged language ("verified by code inspection," "was not independently re-driven end-to-end"), rather than being silently checked as fully proven. `tasks/plan.md` Task 41 (`:189`) and all 8 Checkpoint 12 bullets (`:193-200`) carry the same honest hedging for the two inspection-only items and the browser sign-out path.

## Noted, not graded

- The `/login` autofill-vs-real-bug question remains genuinely unresolved (see above) — flagged as a residual-risk observation per the task's explicit scoping instruction, not a blocking finding. A cheap, low-cost follow-up (one clean re-login attempt with autofill disabled) would close this out but is not required before considering Phase 12 complete, since the code path itself shows no mechanism for the described failure.
- The Step 5 UI stall on "Memproses..." during the deactivate action (report `:91`) was confirmed correct by DB state and page reload rather than by the dialog's own completion signal — a minor UX polish item, not a correctness defect, and appropriately not treated as blocking by the report.

## Review method

Read the task-7 brief and the full task-7 report including the post-report fix section, browser-automation notes, and both inspection-only reachability arguments. Read `supabase/snippets/bootstrap_super_admin_role.sql` in full against `bootstrap_manager_role.sql` line-by-line to independently confirm the hard-fail/idempotency/no-credentials claims rather than trusting the report's prose. Read `supabase/tests/account-lifecycle.test.ts`'s `beforeAll`/`afterAll`/the specific test to confirm the post-report fix's ordering and cleanup are structurally correct. Read the actual `LAST_ACTIVE_SUPER_ADMIN` and `transfer_active_ownership` SQL in `supabase/migrations/20260718180929_add_account_lifecycle_functions.sql` at the exact line numbers the report cites, confirming self-check-before-guard ordering in all three lifecycle functions and validation-before-mutation ordering in the transfer function. Read `supabase/migrations/20260718160405_add_account_status_and_active_role_guard.sql` to independently confirm the `profiles_select_own_or_privileged` fail-closed claim. Read `src/routes/login.tsx` and `src/context/role-context.tsx` in full, specifically hunting for any code-level mechanism that could explain the reported Manager-login-resolves-to-Sales-session anomaly; found none, consistent with (but not proof of) the report's autofill-artifact theory. Diffed `docs/auth-bootstrap.md`, `CLAUDE.md`, and `HANDOFF.md` against the brief's constraints and the system prompt's quoted pre-Task-7 CLAUDE.md text to confirm no overclaiming and that Phase 11 language was left untouched. Cross-checked the plan/tracker checkbox sections against the report's actual evidence to confirm no box was checked without backing. Did not re-run the automated gate, lint, typecheck, or build (per instructions, the controller's independently-reproduced post-fix numbers are trusted), and did not attempt any browser re-verification (per instructions).

## Phase 12 completion assessment

Phase 12 is safe to consider complete. The final task's evidence — both the original report and the controller's independently re-run, post-fix 193/193 result — is internally consistent, the SQL/test changes trace correctly to what's claimed, the two inspection-only lifecycle proofs are backed by code that genuinely supports their conclusions (not just asserted), and the documentation reconciliation neither overclaims (no remote/production language) nor underclaims (hedges are honest and specific). The one open item — the `/login` autofill-vs-bug question — was given real attention here and found to have no plausible code-level explanation as an app defect; it is a reasonable residual-risk note for a future session, not a blocker for closing Phase 12.
