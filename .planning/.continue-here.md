---
context: default
phase: null
task: null
total_tasks: null
status: paused
last_updated: 2026-07-22T15:06:44Z
---

<current_state>
DSM SALES WEB APP V2 is on branch `main`.
Latest pushed commit before this handoff: `9992ab3 fix: update lists immediately after create`.
Remote: `origin https://github.com/Japrang311/dsmsalescrm.git`.

No active `.planning/phases` plan was detected. This is a general session checkpoint.
</current_state>

<completed_work>

Completed Tasks:
- Updated create output behavior so data appears immediately without browser refresh for RFQ, Quotation, Quotation Revision, Prototype Request, Sales Order, and Client creation.
- Added `src/lib/query-cache-updates.ts` to upsert newly created records into exact React Query list caches after successful server writes.
- Added `src/lib/query-cache-updates.test.ts` to prove cache updates use exact query keys and do not corrupt differently shaped caches.
- Exported `toCommercialItem` from `src/lib/data/commercial-items.ts` so created normalized commercial documents can be cached in the existing UI compatibility shape.
- Preserved the user's business rules from the previous session:
  - RFQ does not need or create a number.
  - NP means Non-PPN Sales Order, not New Product.
  - Create Quotation does not include SO number or address.
  - Client creation includes address plus Contact Person 1, 2, and 3.
</completed_work>

<remaining_work>

- No known unfinished code task in this checkpoint.
- If another screen still requires refresh after create/edit, inspect the exact route query key first, then update that exact cache or invalidate it.
</remaining_work>

<decisions_made>

- Use exact React Query cache updates after successful mutation, not broad prefix writes, because several query keys share prefixes but hold different data shapes.
- Keep server refetch via `invalidateQueries` after cache update so the UI is immediate and still reconciles with Supabase.
- Do not add Supabase realtime subscriptions yet; the current request was for the user's own input to appear immediately without manual refresh.
</decisions_made>

<blockers>
- None known.
</blockers>

## Required Reading
1. `AGENTS.md` — project-specific response rules and accuracy posture.
2. `src/components/clients/CreateRecordDialogs.tsx` — create mutation paths for RFQ/Quotation/SO/Prototype.
3. `src/components/clients/AddClientDialog.tsx` — create client mutation path and cache update.
4. `src/lib/query-cache-updates.ts` — exact-cache helper.
5. `src/hooks/use-dashboard-data.ts` — shared list query keys used by Dashboard, Reports, and Sales Orders.

## Critical Anti-Patterns
- Do not add extra form fields or business flows without user approval.
- Do not treat NP as New Product; NP means Non-PPN SO.
- Do not add RFQ numbering back.
- Do not put SO number/address into Create Quotation.
- Do not broad-update query caches with mixed data shapes.

## Infrastructure State
- Dev server was started at `http://127.0.0.1:5173/`, sanity checked with HTTP `307` to `/dashboard`, then stopped.
- Verification completed before `9992ab3`:
  - `bunx tsc --noEmit`
  - targeted tests: 12 pass
  - targeted eslint
  - `bun run build`

<context>
User is enforcing strict business consistency and has explicitly said not to add anything without approval. Keep changes surgical.
</context>

<next_action>
Start with `git status --short && git log -3 --oneline --decorate`. If the user asks for another form/list fix, reproduce the specific screen, identify the query key, then modify only the mutation/cache path needed.
</next_action>
